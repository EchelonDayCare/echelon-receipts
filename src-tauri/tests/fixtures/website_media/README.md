# Website media fixtures directory

Kept empty in the tree — the unit tests inside
`src-tauri/src/website_media/**` generate fixtures at test time (see
`make_solid_jpeg` / `inject_exif_and_iptc` helpers) so we don't have to
commit binary blobs. This README exists so `git` tracks the directory.
